# GemSteps iOS 原生版实施计划

状态：**2026-09-20 原生风格改造已实施，构建及 12 项测试通过；本轮验收与限制见第 9 节。**

当前决策：用户已明确用原生布局、样式与系统弹窗替换 Web 视觉还原，保留手机双列／iPad 多列大方块、全屏遮挡庆祝及新音效。以下第 1–8 节是历史实施记录，其中 Web 配色、固定尺寸、Arial、粒子模拟、旧音效及逐项视觉匹配要求已被第 9 节取代。

参照日期：2026-09-20。Web 基线：`27885e800d5090ad8da6ae3d48884ebaaf5582eb`。
本文件获批后作为执行记录；下方步骤在实际验收前不标记完成。

2026-09-20 review 决策：Gordon 明确排除 iOS PIN，并要求将 **SQLite＋SwiftData** 写入计划作为存储选型。已移除所有 iOS 密码、锁定、会话与家长权限设计；Web PIN 仅作为原产品调研事实保留。采用本地数据库保存逐笔积分记录，取代原每日汇总 JSON 方案，理由见第 3 节。**存储选型已确定；整体实施已于本次用户消息获批。** 日／周统计仅用于评估数据能力，不代表新增统计页面已获批准。

## 1. 范围与调研证据

- 已阅读根目录及 `web/AGENTS.md`；`ios/` 没有更近的 AGENTS.md。根目录说明和 `ios/README.md` 中的“编辑／AI”描述与本轮要求冲突，**以本轮要求为准**，不纳入第一版。
- 第一版：SwiftUI，单设备本地数据，固定中文内容；保持 Web 的任务、奖励、临时加减分、撤销及反馈。启动直接进入任务页，无 PIN 或锁定按钮。
- 不做 PIN／认证／会话／家长与孩子权限，也不为未来权限预留接口。不做任务／奖励编辑、AI、多孩子、多语言、提醒、同步、分组、日程、历史记录或统计页面、Web 数据导入；不涉及 Android。
- 已实际运行现有 Web，而非重新制作 mock UI：`localhost:3107`，单独的 PostgreSQL 17 容器与 `gemsteps_ios_research_test` 数据库，端口 `127.0.0.1:55439`。使用已有依赖和镜像，显式覆盖数据库、测试 PIN、会话密钥；未连接或修改生产数据。
- 浏览器为独立 Playwright 会话，Chromium 153.0.8010.52，iPhone 设备模拟；平板图是同一浏览器改变 viewport，**不是 iOS Safari／真机截图**。只隐藏 Next.js 开发工具浮标，未改应用布局或代码。
- 已操作：错误／正确 PIN、重复任务、奖励不足／足够、任务／奖励撤销、余额不足撤销、临时加减与非法输入、刷新恢复、跨天、离线记分、离线锁定、重新联网解锁、滚动及不同宽度。
- 现有测试：`npm run test:run` **94/94**；隔离库中的 `npm run test:integration` **19/19**。后者包括余额约束、并发兑换／撤销及数据库保护。测试通过不代表 iOS 已验证。
- 浏览器、开发服务器和临时数据库均已停止；临时容器已移除。本轮只有计划、截图及截图索引，没有 Xcode 工程、应用代码或依赖变更，没有提交／推送／部署。

主要源码证据：

| 内容 | 直接参照 |
|---|---|
| 固定目录、分数、洛杉矶日期 | [points.ts](../web/src/lib/points.ts) |
| 主界面、操作反馈 | [PointsPage.tsx](../web/src/app/PointsPage.tsx)、[points/](../web/src/app/points/) |
| 日期刷新、数字动画、点击控制 | [usePointsController.ts](../web/src/app/points/usePointsController.ts) |
| 权威积分限制 | [point-ledger.ts](../web/src/lib/server/point-ledger.ts)、[数据库迁移](../web/prisma/migrations/20260825000000_add_point_balance/migration.sql) |
| 本地缓存和离线行为 | [offline-db.ts](../web/src/lib/offline-db.ts)、[sync-controller.ts](../web/src/lib/sync-controller.ts) |
| PIN、会话、显式锁定 | [ProtectedApp.tsx](../web/src/app/ProtectedApp.tsx)、[auth.ts](../web/src/lib/auth.ts)、[offline-auth.ts](../web/src/lib/offline-auth.ts) |
| 回归依据 | [现有测试](../web/src/__tests__/) |

[全部参照截图及数据说明](../output/playwright/gemsteps-ios/README.md)。优先 review：[手机任务](../output/playwright/gemsteps-ios/08-tasks-earned-390.png)、[奖励撤销](../output/playwright/gemsteps-ios/10-undo-reward-390.png)、[临时减分校验](../output/playwright/gemsteps-ios/13-adjustment-insufficient-390.png)、[全部 32 项任务](../output/playwright/gemsteps-ios/16-tasks-ipad-full.png)、[全部 9 项奖励](../output/playwright/gemsteps-ios/17-rewards-ipad.png)。

## 2. 当前 Web 产品与 iOS 保留范围

下表记录实际 Web 行为。PIN／会话两行仅为调研事实，按 Gordon 的明确指示不移植；其余行为除明确列出的差异外保留。Web 的公开 URL 访问限制不等同于本地 App 的产品需求。

| 页面／状态 | 实际行为 |
|---|---|
| PIN 页 | 六位数字，输满自动验证；自带 0–9 和删除键。错误后清空并提示“密码不正确，请再试一次”。没有注册、设置 PIN、修改 PIN、找回密码或生物识别。 |
| 主页面 | 默认任务页；顶部显示累计余额、月日、中文星期及当日净变化。任务／奖励是顶部切换按钮，不是底部导航；页头随整页滚动，不吸顶。没有日期选择器。 |
| 任务 | 32 项，整张卡片点击加分；可重复完成，无每日次数上限。角标是**当天净完成次数**，不是累计次数或复选状态。 |
| 奖励 | 9 项，直接点击兑换，无二次确认。每次扣固定分数并增加当天次数；余额不足禁用，够分可重复兑换。 |
| 撤销模式 | 持续模式，可跨两个标签；每点一次撤销该卡片当天一次操作，不是全局“撤销最后一步”。未发生过的卡片禁用。任务撤销扣分；奖励撤销退分。卡片仍显示原来的 `+n 分`／`-n 分`，不改文案方向。退出模式恢复普通操作。 |
| 临时加减 | 独立弹层，默认加 1；快捷值 1、2、3、5、10；每次允许 1–100 整数，禁止 0、空值、小数、超范围。减分不能超过当前余额，余额 0 时减分选项禁用。影响余额与当日净值，不改变卡片次数；没有专门的临时操作撤销入口，可反向调整。撤销模式下仍能使用它。 |
| 关闭弹层 | ×、背景点击或 Escape 可关闭；保存中不能关闭／重复提交；保存成功关闭并反馈，失败保留弹层和输入。 |
| 保存／刷新 | Web 先写 IndexedDB，再同步服务器；刷新后保留积分和计数，但回到任务页、退出撤销并关闭弹层。显式锁定不清除积分。 |
| 跨天 | 固定 `America/Los_Angeles`，不是设备时区。总余额跨天保留；新日期的次数和当日净值从 0 开始。Web 前台每 10 秒以及 focus／visibility／联网时检查。当天净值允许为负，总余额不允许为负。 |
| 会话 | 正确 PIN 签发 30 天期限，重新载入时检查；手动锁定持续到再次成功解锁。没有离开页面就自动锁定的产品规则，也没有主动到期倒计时界面。 |

积分不变量：只接受固定目录的 ID 和分值；余额 ≥ 0；各日任务／奖励次数 ≥ 0。没有额外每日积分上限，也没有用户可见的累计积分上限。计数与余额同时成功或同时失败；拒绝的动作不算一次操作。

**已观察到的 Web 特例，不能隐瞒：**完成洗脸两次、刷牙一次得到 5 分，兑换棒棒糖后余额 0；此时仍可点“撤销刷牙”，Web API 返回 409，最终余额仍为 0、刷牙次数仍为 1，无明确拒绝提示。源码中的离线乐观路径没有相同的余额校验，因此存在离线暂时负数的风险；本轮没有把这一风险当作已完成的离线实测。iOS 推荐保存前拒绝，保持原值，并在已有页头错误位置提示“积分不足，无法撤销”；这项可见差异列入 review。

### 视觉和反馈

- 固定浅色；紫—靛—蓝渐变页头、金币、浅色渐变背景、彩色圆角卡片。卡片 165×165、圆角 16；任务按数组索引循环 10 种渐变，奖励按 `id.length % 10` 取色，不能按奖励位置重新配色。
- 任务外边距 24、卡片间距 12、左对齐换行；390 宽为两列，375 宽为一列。奖励使用最小轨道 150、间距 12、卡片仍为 165 的网格，列间距与任务不同；375 宽仍为两列且较挤。834 宽两页均四列，但横向间隔不同；1194 宽奖励七列，卡片几乎贴合（见横屏截图）。**不顺手改成统一网格。**
- Web 页头在 640 宽以下分两行；640 及以上同一行。iOS 删除锁按钮和它的 44 宽占位，窄屏操作行由“撤销模式／临时加减”两个按钮平分，其余视觉参数沿用。临时弹层窄屏靠底，宽屏居中、最大宽 448；文字、间距、图片大小以源码和截图共同对照。
- 成功普通任务／兑换／临时调整出现全屏白色 88% 遮罩和模糊，**阻挡后续点击约 2.2 秒**；16 个图标粒子下落，中心显示绿色正数或玫红负数。任务粒子优先用其 PNG；奖励粒子用配置 emoji，即使卡片使用 PNG；临时调整用 ⭐。撤销不播放庆祝或声音。
- 中心数字淡入淡出约 1.8 秒、弹跳 0.7 秒；积分变化通常在 600ms 内逐步更新，最多 30 步。Web 从显示值 0 更新时会直接采用新余额，这是源码中的例外，不应承诺每次都滚动数字。
- 音效没有现成音频文件：Web 合成六个正弦音，392、440、523.25、587.33、659.25、783.99 Hz；每隔 0.25 秒起音，最后一个延长，总长 1.7 秒。加分上行，扣分下行；音频失败不影响保存。参数已读源码，**真实听感未完成听音比对**。

Web 专属技术不移植：PIN 页与锁定入口、认证与会话、Next.js 路由与 API、PostgreSQL／Prisma／触发器、cookie/HMAC、IndexedDB 缓存、outbox／重试／同步、轮询远端、PWA 安装更新与 Service Worker、Next Image 优化及开发浮标。加载／读写失败的用户反馈保留本地对应状态；不模拟网络延迟或远端错误。

## 3. 明确推荐的最小 iOS 方案

**已确定的存储选型：SQLite＋SwiftData。** 使用 SwiftUI、一个 `@MainActor @Observable` 状态对象和系统 SwiftData，保存本地逐笔积分记录；零第三方依赖。最低 iOS／iPadOS 17。本次已获整体开发授权。

具体采用 SwiftData 的默认本地持久化方案：Swift 模型 → SwiftData → Core Data → SQLite 数据库文件，保存在 App 沙盒内。应用通过 SwiftData 读写，不直接操作底层 SQLite 表，也不另建一套 SQL 访问层或自定义存储。

Schema 在 Swift 代码中用 `@Model` 定义 `PointEntry`，声明字段类型、可选性及必要约束；`ModelContainer` 根据模型管理存储结构。之后若改变模型，使用框架支持的迁移机制；复杂变更再明确版本和迁移步骤，不假设所有变化都能自动兼容。第一版只需一个持久化模型，不预建迁移框架、仓储协议、服务容器、跨平台共享层或事件同步框架。

### JSON 与数据库：复核结论

`Codable` 是 Swift 数据的编码／解码机制，不是数据库，也不决定保留什么信息。需要分别决定“存储引擎”和“保存每日汇总还是每次操作”。JSON 可以存逐笔明细；数据库如果只存汇总，同样无法还原明细。

原 JSON 方案存了每日任务次数、奖励次数及临时调整净额，因此**今天、上周的任务净得分／兑换净支出／总净变化都能计算，并不困难**：取出相应日期，做乘法和求和。上周只需取七个日汇总；不能说没 SQL 就做不了统计。当前固定目录前提下，十年也只有约 3,650 个日汇总，不能仅凭数据量断言需要数据库。

真正的不足是汇总不可逆：

- 临时加 10 再减 8 与仅加 2 都只留下净额 +2；不能再分别回答“临时奖励多少、扣了多少”。
- 完成两次再撤销一次，与只完成一次都留下次数 1；不能再区分原始操作和撤销过程，不能按小时统计或追查一次误操作。
- 原方案用“次数 × 当前目录分值”重算历史；若将来发布新版本调整固定分值，旧积分会被重算。第一版虽然不支持编辑，保存动作当时的实际分数仍比依赖未来目录更稳妥。
- JSON 后来可以迁入数据库，但只能迁已有信息，不能找回已经丢弃的明细。数据库也不会自动修正上述建模问题。

| 比较项 | 每日汇总 JSON（原方案） | 逐笔 JSON | SQLite＋SwiftData 逐笔记录（已选定） |
|---|---|---|---|
| 当天／上周汇总 | 简单，取日汇总求和 | 按日期和类型筛选、求和 | 按日期和类型 fetch，再计算所需汇总 |
| 时段／操作明细、临时加减拆分 | 已丢失的信息无法恢复 | 可做，但自行遍历／组织查询 | 可筛选、排序和分批读取，不必全读历史 |
| 保存动作 | 原子重写整个汇总文件 | 单文件方案随历史增加而重写更多数据 | 由持久化引擎保存新增记录，无需应用重编码整份 JSON |
| 一致性 | 单写入者＋原子替换可满足当前需求 | 同左；不能把普通 append 当作事务 | 明确 save／失败 rollback；业务规则仍须自己校验 |
| 字段变更／维护 | 自己处理解码兼容与格式升级 | 同左 | 有 schema／迁移机制，但语义变化仍需设计与测试 |
| 代价 | 起步代码最少，信息损失最大 | 保留明细，但文件读写与查询维护增多 | 多一个 model、container/context 和持久化测试；有框架版本约束 |

如果唯一目标永远是现有界面加简单七日总分，JSON 完全够用。结合本次对历史统计的明确关切，以及 Web 本来就保留逐笔 `PointEntry`，本计划选择保留同等粒度，用 SQLite＋系统 SwiftData 管理本地数据；**原因是保留信息并减少持续维护文件存储的工作，不是宣称 JSON 无法查询，也不是为 AI／同步／多用户预建架构。**

SwiftData 不等于直接写 SQL。以 iOS 17 可用能力为基线，计划使用 `FetchDescriptor`／predicate 筛选，再以 Swift 求和；不能承诺任意 `SUM/GROUP BY/JOIN` 都能作为一个 SwiftData 查询下推。若目标是直接写复杂 SQL 报表，SQLite 更合适：它原生支持 `SUM` 等聚合及事务，但直接接系统 SQLite C API 还要处理语句、参数绑定、结果映射和 schema 升级。Core Data 也能胜任，然而本任务没有必须选它的具体需求；不同时叠加两套存储栈。

统计口径必须独立说明：例如任务得 20、兑换支出 15、临时加 3／减 2，当天任务得分为 20，净变化为 +6。撤销任务应冲减任务得分，奖励退款不能算完成任务所得。“上周”还需定义周起始日，日期边界均按 PT，使用起点包含／终点不包含的区间。数据库查询也不能替代这些业务定义。本轮不加统计入口，也不提前要求 Gordon 决定未开发报表的口径。

性能边界不夸大：假设每天 50 次操作，一年约 18,250 条、十年约 182,500 条，这是估算而非实测。当前推荐启动时分批汇总余额、日常操作只增量更新内存结果；不每次点卡片都全表重算，不预建永久汇总表。实际启动时间需要实现后测量，有证据再决定是否需要优化。

### 页面组织

启动加载本地数据后直接显示任务页；没有密码页、锁按钮、登录状态或角色概念。主页面使用一个 ScrollView，内含页头、顶部双标签和卡片；标签、撤销模式、弹层和庆祝是视图临时状态。卡片共享一个小视图，任务／奖励保留各自布局参数。弹层用 SwiftUI overlay 还原靠底／居中的 Web 位置，不引入原生 sheet 的拖拽档位或新导航。

iPhone、iPad 使用同一界面，按**可用内容宽度**重排；iPad 支持横竖屏及窗口缩小，不增加侧栏、多窗口或额外面板。安全区由系统处理，背景延伸到边缘；默认字号下对照 Web，文字用 Arial 可用字形及系统中文／emoji 回退，保留浅色外观。较大辅助字号允许文字与布局必要扩展，不以截图一致为由裁切关键操作。

### 数据和保存

- `Catalog`：静态 Task／Reward 数组，逐项保留 Web 的 ID、标题、顺序、emoji、分数和图片映射；不提供编辑入口。不把测试夹具中简化／过时的标题当正式目录。
- 唯一持久化 model 为 `PointEntry`：UUID、发生时间 `occurredAt`、PT `dateKey`、类型（task／reward／adjustment）、固定 `itemID`、实际有符号 `points`。任务完成为正、任务撤销为负；兑换为负、退款为正；临时调整按其符号记录。只接受当前目录的分值，不另存任务／奖励数据库表。
- 每次成功操作新增一条记录；撤销记录反向分值，不删除原记录，沿用 Web 的业务语义。当前撤销是“同项当天一次”，不引入指定历史操作撤销、撤销链或历史浏览功能。
- 总余额为全部记录 points 之和；当日净值为该 dateKey 的 points 之和；当日任务次数为正向条数减撤销条数，奖励次数为兑换条数减退款条数。未发生项目为 0；历史分数不从当前目录反推。
- 一个本地 `ModelContainer`、一个写入 `ModelContext`，显式关闭该 context 自动保存，使用明确的 `save()`。状态对象串行执行：取 PT 日期 → 根据已保存状态校验 → 插入记录 → save → 更新内存余额／次数 → 反馈。失败 rollback 未保存插入，展示错误，保留原显示值，不庆祝；不直接将 context 的未保存值绑定成积分 UI。
- 启动时从记录重建余额和今日计数；正常操作后只增量更新内存值，跨天重新读取当日记录。内存状态是可重建的显示结果，不增加持久化余额表、缓存同步机制或远端事件队列。
- 首次创建空库才从零开始。数据库打开／读取失败显示加载失败，**不自动删库重建或清零**。测试使用独立临时数据库；不建设备份／恢复平台。
- 显式配置本地存储（CloudKit database 为 none），不申请 iCloud 同步能力；系统设备备份不等同于产品同步。第一版安装后从零开始，不搬运 Web 生产数据。由于 iOS 尚未开发，无需编写 JSON 到数据库的迁移代码。
- 日期用 Gregorian Calendar + `America/Los_Angeles`；启动、回前台、每次动作和前台每 10 秒检查日期，后台无需运行。仅更新当天投影，不清空历史／总余额。夏令时由系统时区处理，不写死 UTC−7／−8。测试传入 `Date` 即可，不建时钟框架。

### 素材和反馈

- 原样复制 `taskImages.ts` 指向的 **7 张**及 `rewardImages.ts` 指向的 **5 张** PNG 到 Asset Catalog；保留透明度和宽高比，使用各卡片现有显示框尺寸。不复制未使用的 JPG／旧版车载电视图，也不替换成 SF Symbols 或新插画。
- 金币用少量 SwiftUI 圆形、渐变、阴影及 ★ 还原；emoji 保留原字符。色值从当前编译 CSS 提取，不能拿另一版本 Tailwind 的近似色表代替。
- 按 `pointSounds.ts` 的频率、正弦波、音量包络和时长一次性生成两份 WAV，随包保存，`AVAudioPlayer` 播放。无需运行时音频合成框架；生成参数记在资源说明中。
- SwiftUI 内实现现有遮罩、16 粒子和数字反馈，不加动画包。音频用 `.ambient`，尊重静音开关；声音异常不阻塞积分。系统“减弱动态效果”下简化运动，保留成功信息和交互阻挡，无新增用户设置。
- 现有应用图标只有 180×180：内部开发先沿用相同图形，按 Xcode 所需尺寸适配；发布规格／高清源图尚未核实，不能把放大描述成高清。不会为此擅自生成新品牌图标或扩大本轮范围。

系统能力依据：[SwiftUI Observation](https://developer.apple.com/documentation/swiftui/monitoring-model-data-changes-in-your-app)、[SwiftData 持久化](https://developer.apple.com/documentation/swiftdata/preserving-your-apps-model-data-across-launches)、[筛选与排序](https://developer.apple.com/documentation/swiftdata/filtering-and-sorting-persistent-data)、[显式保存](https://developer.apple.com/documentation/swiftdata/modelcontext/save%28%29)、[失败 rollback](https://developer.apple.com/documentation/swiftdata/modelcontext/rollback%28%29)、[AVAudioSession ambient](https://developer.apple.com/documentation/avfaudio/avaudiosession/category-swift.struct/ambient)。SQLite 对照依据：[聚合函数](https://www.sqlite.org/lang_aggfunc.html)、[原子提交](https://www.sqlite.org/atomiccommit.html)。只用最低系统版本可用的 API，不因文档展示较新特性而提高最低版本；本机尚无 Xcode，编译验证留待获批后。

### 预计新增文件（批准后才创建）

| 路径，相对 `ios/` | 职责 |
|---|---|
| `GemSteps.xcodeproj`、`GemSteps/GemStepsApp.swift` | 单 App target、入口和根状态；单测试 target。 |
| `GemSteps/Models/Catalog.swift`、`PointEntry.swift`、`PointsState.swift` | 固定目录、唯一持久化记录模型、内存积分／次数计算和动作校验。 |
| `GemSteps/AppState.swift` | 当前日期、加载／保存调用、操作串行化、成功与失败结果。 |
| `GemSteps/LocalStore.swift` | 配置本地 ModelContainer／context，读取积分记录和明确保存／失败回滚，不抽象成协议层。 |
| `GemSteps/Views/PointsView.swift` | 加载／错误／主页面，页头、标签及任务奖励排布。 |
| `GemSteps/Views/PointCard.swift`、`AdjustmentView.swift`、`CelebrationView.swift` | 卡片、临时加减弹层、现有庆祝。小型页头子视图可留在 PointsView 同文件。 |
| `GemSteps/PointSound.swift`、`Assets.xcassets/`、`Resources/Audio/` | 系统音频播放、原图片、两份生成音效；视觉常量放使用处或一个小型 `Appearance.swift`。 |
| `GemStepsTests/PointsTests.swift`、`LocalStoreTests.swift` | 下列业务边界和独立临时数据库持久化；实现后添加，不做 TDD。 |

## 4. 有限的验收范围

### 功能场景

| 编号 | 验收动作与明确结果 | 方法 |
|---|---|---|
| F1 | 初装直接进入任务页、0 分；无 PIN 页、锁定按钮、认证请求；飞行模式启动和全部积分操作正常。 | 模拟器操作 |
| F2 | 洗脸×2、刷牙×1 → 总分／当天净值 5，次数 2／1；无额外每日限次，不能因快速点击或保存重入重复扣加。 | 手动操作 + 业务测试 |
| F3 | 5 分只能兑换棒棒糖；兑换后余额 0、兑换次数 1；再次普通兑换禁用；奖励撤销后余额 5、次数 0。 | 手动操作 + 业务测试 |
| F4 | 接 F3 撤销洗脸一次 → 4 分、次数 1；未做的任务不能撤销。另测余额 0 而刷牙次数 1，拒绝扣 3，值不变；无庆祝。 | 手动操作 + 业务测试 |
| F5 | 临时加／减、五个快捷值、1 与 100 边界；拒绝空、0、小数、101、超余额；只改积分，次数不变；关闭／取消不保存。 | 模拟器操作 + 参数化业务测试 |
| F6 | 前日余额 12，PT 次日开始仍为 12、当日 0、全部次数 0；次日兑换棒棒糖 → 7／−5；旧日次数不可从今日撤销。前台跨午夜和后台恢复各测一次。 | 固定日期测试 + 模拟器操作 |
| F7 | PT 夏季午夜 `2026-09-21T07:00Z`、冬季午夜 `2026-12-15T08:00Z` 前后边界；设备改为其他时区不改变 PT 归日；日期回拨从已有记录重建当天汇总，不重新奖励。 | 业务测试 |
| F8 | 杀进程／重开保留余额和次数，回到任务页／正常模式；保存失败不发布候选值、不残留待保存操作；数据库打开失败不清零。逐笔记录保留临时 +10／−8 与撤销的各自实际分值；按日期读取再汇总结果正确，不增加统计 UI。 | 临时数据库测试 + 一次模拟器重启 |
| F9 | 加分上行音／正数庆祝，扣分下行音／负数庆祝，撤销静默；图片任务粒子、奖励 emoji、临时 ⭐；2.2 秒遮罩拦截额外操作，随后恢复。 | 操作、短录屏／定时截图；真机听音 |
| F10 | 全部 32／9 项内容、ID、顺序、分数、图标映射一致；长标题、滚动到底部、切标签、不同宽度、默认／较大字号可用。 | 静态目录核对 + 截图／模拟器 |

不建立通用截图平台、设备农场或复杂端到端框架。业务测试用 Xcode 自带测试能力；视觉用现有浏览器、Simulator 操作及截图，必要时加少量直接 UI 操作验证，不为一次对照建设基础设施。

### 相同数据、日期、尺寸的视觉对照

固定数据采用截图索引中的 S0–S6；Web 用隔离库从零执行指定动作，iOS 用同样动作或仅 DEBUG 可用的几份固定初始数据。日期通过浏览器 clock、iOS 测试入口固定 `Date`；只需几个明确启动参数，不做通用 fixture／时间控制系统，不进入 Release。

| 有限视觉状态 | 对照范围 |
|---|---|
| V1 移除 PIN 后的启动与页头 | 390 内容宽；直接进任务页，无锁图标或空占位；两个操作按钮平分原操作行。 |
| V2 任务全零／重复完成／撤销 | 390×844；全部任务用 834 宽全页参照补足，不只看首屏。 |
| V3 奖励不足／5 分可兑／已兑撤销 | 390×844；全部奖励补充 834×1194。 |
| V4 临时加减默认／非法数值／余额不足 | 390×844 底部弹层，834×1194 居中弹层。 |
| V5 跨天与当日负值 | 390×844，验证头部数字、颜色、日期与次数。 |
| V6 成功反馈 | 普通图片任务、奖励、临时调整各一轮；取成功后约 0.7 秒画面，并验证 2.2 秒释放交互。 |
| V7 尺寸边界 | 375×667 窄屏、834×1194 平板、1194×834 横屏；不把每种状态乘以全部设备形成无边界矩阵。 |

以上是 **CSS 像素／SwiftUI point 的内容视口**，不是简单把整张 iPhone 截图拉伸成浏览器尺寸。原生截图记录实际安全区和内容矩形；浏览器设成相同内容宽高后重拍对应状态，原生裁去系统状态栏／Home 指示条并按 scale 换算为 1×，禁止非等比拉伸。现有 390×844 等截图是初始基线，若模拟器内容高不同，仅重拍受影响状态。系统版本、字号、浅色、滚动位置及 Reduce Motion 关闭保持一致；静态图等动画结束，动态图按时间点对照。

必须完全一致：目录文字／顺序／分数／素材选择、积分最终值、合法操作条件、当天次数、PT 归日、反馈触发条件，以及移除 PIN／锁入口之后的控件组织和上述排布规则。PIN 与锁定是 Gordon 明确要求的删除项；Web 截图保留原样作为证据，01／02 密码图不再是 iOS 验收目标，其他截图中的锁按钮也不照搬。不得另以“原生”作理由改内容、增加确认步骤或移走功能。

允许并记录的原生差异：系统安全区、键盘与焦点行为、文字／emoji 的系统版本差异、字形栅格化、阴影／模糊和渐变插值细微差别、音频路由／静音开关、辅助字号和减弱动态效果。目标是可审查的视觉匹配，不承诺跨渲染引擎逐像素完全相同。

## 5. Review 决策与未验证项

已确定：完全排除 PIN，不预留家长／孩子角色设计；存储采用 **SQLite＋SwiftData＋逐笔 PointEntry**，取代每日汇总 JSON。不增加统计／历史页面，不复制服务器同步架构。这两项无需再次确认；整体开发已获批准，第 5 节具体推荐仍单独待确认。

仍需 review 的具体推荐：

1. **设备范围与排布**：最低 iOS／iPadOS 17；确认实际使用设备能运行该版本。推荐按 Web 保留 375 宽任务单列／奖励双列，以及横屏奖励卡片较密的现状，不擅自“优化”成统一网格；可从窄屏和横屏截图直观看是否接受。
2. **余额不足的任务撤销**：批准前述本地直接拒绝＋页头提示，替代 Web 无提示的服务器回滚；离线也保持余额非负。除此之外不把调研发现变成 Web 修复任务。

当前未验证／限制（2026-09-20更新；初始环境记录留在第8节时间线）：

- Xcode27与iOS27 runtime现已安装，构建、测试、三种模拟器启动均通过；没有再以CLT缺失为阻塞。仅最低部署目标17，未在iOS17实际运行。
- Web比较仍是Chromium，同机型内容尺寸对齐；未在iOS Safari额外重复全部操作。
- 真机音效听感、静音／蓝牙／音频中断、飞行模式冷启动未验证；没有在个人真机安装。
- 较大字号已检查；减弱动态效果、极端字号完整操作未验证。Escape未能通过Device Hub送达应用；最新弹层在SE软件键盘下复测未完成。
- 原生真实SQLite只读写失败及损坏不覆盖测试通过，但未模拟真实磁盘满。Web认证/PWA专属行为不在移植范围。
- 现有180×180图标放大用于开发，未确认发布素材品质；上架和发布不在本轮。
- 额外不足撤销提示未启用，不把未回复的第5节推荐当作批准。网格按用户“Web唯一参照”保留。

## 6. 获批后的四个验收步骤

| 步骤 | 产出 | 验收及状态 |
|---|---|---|
| 1. 最小工程与固定界面 | 已按本次授权创建单 App／测试 target；原生构建与iPhone／iPad启动已通过，移入固定目录和 12 张图片，完成无 PIN／无锁按钮的主页面与弹层静态状态。 | 构建并在 iPhone、iPad 启动；V1–V4、V7 初对照；无编辑等额外入口。**实施中；已进行部分原生验收，完整结果见第8节末及native索引** |
| 2. 本地业务闭环 | SwiftData 逐笔记录、明确保存、加减／兑换／撤销／跨日和重启恢复。 | 先实现后补验证，F1–F8、F10；不变量和故障路径通过；记录实际规模下的启动加载表现。**实施中；已进行部分原生验收，完整结果见第8节末及native索引** |
| 3. 视觉与反馈还原 | 调整间距／颜色／图片、数字动画、庆祝、两种音效及系统适配。 | V1–V7 对照和 F9；列出可见差异与真机未完成项，不以截图存在代替检查。**实施中；已进行部分原生验收，完整结果见第8节末及native索引** |
| 4. 有限验收与交付 | 跑上述有限功能集，重拍受修改影响状态，记录结果、截图与剩余差异。 | 更新本计划，列出通过／未通过／未验证；无未说明的业务差异。**实施中；已进行部分原生验收，完整结果见第8节末及native索引** |

普通修复直接局部处理：卡片参数、字体、素材框、校验条件、日期边界和单个回归测试，无需重复审批。若必须显著改变本计划的数据／状态架构、加第三方依赖、建设新基础设施或扩大产品范围，先记录“复现证据 → 原因 → 最小替代方案 → 影响”，等待 Gordon review，不先实施再补批准。不能为了像素一致扩大工程，也不能以控制范围为由隐藏差异。

## 7. Web 对照、修复与再验证循环（开发获批后执行）

**交付目标是通过对照验收的 App，不是仅完成第一轮编码或成功构建。** Agent 应在已批准范围内持续执行下列循环，普通局部修复不需要 Gordon 每轮重新批准。

1. **确认覆盖清单。** 将第 2 节每项保留的用户功能和行为映射到 F1–F10，将界面及反馈状态映射到 V1–V7。包含成功、禁用、拒绝、撤销、持久化、跨天和失败路径，不能只验收顺利操作的首屏。若发现遗漏的现有 Web 行为，补充对应场景；这属于补齐既有范围，不是新增产品功能。PIN／锁入口明确排除。
2. **实现并运行。** 完成一个可操作的实现步骤后构建，在模拟器实际运行；业务测试通过不能替代 UI 操作和视觉检查。全流程在实现闭环后重放，不等到交付时才首次核对。
3. **同条件对照 Web。** 使用第 4 节规定的相同数据、PT 日期、内容尺寸和操作序列，在隔离 Web 环境与 iOS 上分别操作。核对积分、次数、可点击条件、弹层、跨天、重启与反馈；截图比较控件位置、卡片大小、间距、文字、颜色及素材，动画与声音另按时序和实际播放验证。Web 不连接生产数据。
4. **记录并修复差异。** 在本文件维护简短验收表：`场景 ID｜Web 预期｜iOS 实际｜证据路径｜状态`，状态为“通过／待修复／未验证／已接受差异”。定位差异原因，优先修改对应视图参数或业务逻辑；不能通过改 Web 参照、放宽预期或删除测试掩盖不一致。
5. **复测后继续循环。** 修改后重跑失败场景、受影响的相关场景及必要业务回归，重拍受影响状态。若仍不一致，继续定位、修改和复测；已稳定通过且未受影响的场景无需每轮重复。最终候选版本完成一次完整 F／V 清单核对，汇总证据后交付。

### 循环结束与交付条件

- 覆盖清单中的保留功能无遗漏；功能结果和交互行为通过，不能用“基本可用”代替验收。
- 视觉达到同类布局和相近外观：没有错误素材、明显错位、截断、遗漏、错误状态或明显颜色／尺寸差异；不要求跨平台字形和模糊逐像素相同。按第 4 节记录不可避免的系统差异。
- “待修复”项清零；任何可见行为偏离都必须有明确依据。PIN 移除已确定，其他产品行为差异不能由 Agent 自行标成“已接受”。余额不足撤销的额外提示仍是待 review 建议，不能因进入修复循环而自动获批。
- 必须验收项如果因模拟器、真机、听音或其他环境条件无法执行，标记“未验证”，说明阻碍及需要补做的操作；可先交付进展供 review，但**不得声称整体完成或全部一致**。修复可推进的部分继续进行，不反复重跑无法执行的检查。
- 不为循环设定“修三轮就算完成”这样的次数上限，也不为追求无法消除的系统像素差异无限修改。若证据表明需要实质性架构变更、依赖、新基础设施或产品扩展，按第 6 节提交证据和最小替代方案，等待 review；普通局部修复持续自主完成。

执行记录：2026-09-20 开始实施，主 checkout / main，初始工作区干净。重新检查仍只有 CommandLineTools，无 Xcode / simctl；继续实现可推进部分，原生验收不记通过。

**决策记录：PIN 已排除；SQLite＋SwiftData 已确定；整体实施已授权。第 5 节设备／排布／额外拒绝提示的确认独立处理。**


## 8. 本次实施与验收记录

2026-09-20，用户明确授权创建 Xcode 工程和实现；不提交、不推送、不部署、不发布。
主 checkout `main`，起始状态干净。未修改 Web 产品代码或 Android。

### 实施状态

- 步骤 1：已创建单 App / XCTest target、共享 scheme、固定目录、12 张原样 PNG、主页面和自定义临时弹层。工程 plist 校验及 Swift 语法解析通过；**iPhone / iPad 构建启动与视觉验收未验证**。
- 步骤 2：已实现 SwiftData 单一 `PointEntry`、关闭自动保存、显式 save / rollback、串行操作、保存成功后发布、PT 日期检查、增量余额与当天计数。XCTest 在实现后添加；**持久化、重开、故障回滚的实际执行未验证**。
- 步骤 3：已实现不同网格、卡片、页头、600ms 数字递进、16 粒子、2.2s 交互遮罩、两份 1.7s WAV、Reduce Motion 分支；**原生动画／声音／辅助字号实测未验证**。
- 步骤 4：完成本机可运行检查、隔离 Web 重放与证据记录；**整体交付未完成**。未把首轮实现、语法解析或核心检查当作 App 验收。

### 环境与实际检查

- `xcode-select -p` → `/Library/Developer/CommandLineTools`；`xcodebuild -version` 失败（需要 Xcode），`xcrun simctl list devices` 失败（没有 simctl）；`/Applications` 无 Xcode。
- Swift 6.4 / arm64 macOS CLT 可编译 Foundation 核心；实际执行独立 157 项断言通过，`TZ=Asia/Shanghai` 再跑 157 项通过。源码与复现命令见[本次证据](../output/playwright/gemsteps-ios/implementation/README.md)。这是核心逻辑检查，不是 XCTest 或 iOS 运行。
- `swiftc -frontend -parse` 全部 App / 测试 Swift 文件通过；`plutil -lint` 工程通过。`PointCard.swift` 与目录使用 macOS SwiftUI SDK 单独 typecheck 通过，不能替代 iOS target 构建。
- 尝试 SwiftData typecheck，因缺少 `SwiftDataMacros` 插件失败。没有将失败绕过后标成成功；XCTest 7 个方法未执行，iOS SDK、资产编译、签名、安装、原生截图均未验证。
- Web `npm run test:run` 94/94；独立 PostgreSQL 17 的 `npm run test:integration` 19/19。目标 `localhost:55439/gemsteps_ios_implementation_test`，容器 `gemsteps-ios-implementation-db`；Next 仅绑定 localhost:3107，显式覆盖测试数据库、临时 PIN 与会话密钥。未读取或复制生产数据库。
- 41 个目录项 ID / 标题 / emoji / 分数 / 顺序逐项匹配；12 张卡片图片与 Web 原文件字节一致。应用图标由原有 180 图等比放大为 1024，仅开发使用，发布品质未确认。
- 使用 Apple 官方 [ModelConfiguration](https://developer.apple.com/documentation/swiftdata/modelconfiguration/init(_:schema:url:allowssave:cloudkitdatabase:)) 与 [enumerate](https://developer.apple.com/documentation/swiftdata/modelcontext/enumerate(_:batchsize:allowescapingmutations:block:)) 核对持久化配置和批量遍历 API；API 文档核对不是运行证据。

### 对照循环与差异

| 场景 ID | Web 预期 | iOS 实际／当前证据 | 证据路径 | 状态 |
|---|---|---|---|---|
| F1 | 无 PIN 后从零进入任务，离线可操作 | 无认证／网络代码，空库路径已实现；未安装运行 | `ios/GemSteps/GemStepsApp.swift` | 未验证 |
| F2–F4 | S1=5，S2=0，S3=4；拒绝余额不足撤销，不变更次数 | Foundation 实际结果符合；原生点击与 SwiftData 保存未运行 | `implementation/core-check.swift`、Web S1/S2 图 | 核心通过；原生未验证 |
| F5 | 1–100、非零整数、超余额拒绝；临时调整不改变次数 | 核心边界通过；弹层代码包含空／小数／范围校验，但 UI 未运行 | Web S3-invalid 图、`PointsTests.swift` | 核心通过；UI 未验证 |
| F6–F7 | PT 次日余额12／当天0；兑换后7／−5；夏冬午夜与回拨 | Foundation 午夜／DST／设备时区检查通过；后台恢复、10秒前台跨日与库回拨未运行 | Web S4/S5 图、核心检查、`LocalStoreTests.swift` | 核心通过；原生未验证 |
| F8 | 保存失败无候选值、重启恢复、损坏不清零 | 已写临时真实磁盘库 XCTest；SwiftData 宏环境阻塞 | `LocalStoreTests.swift` | 未验证 |
| F9 | 16 粒子／2.2s遮罩／加减声音／撤销静默 | 实现与源码参数核对；Web庆祝截图已拍；原生时序／真机听音未验证 | Web S6-celebration 图、Audio README | 未验证 |
| F10 | 32／9目录和12图片一致，尺寸切换与滚动可用 | 目录及图片自动比对通过；原生滚动／大字号未验证 | `Catalog.swift`、Assets | 静态通过；原生未验证 |
| V1–V5、V7 | 同数据、日期、内容宽高；保留各自排布 | 已实现初稿；没有原生截图，不能做同尺寸成对比较 | `implementation/web-*.png` | 未验证 |
| V6 | 同操作0.7s反馈及2.2s释放 | Web重拍，原生缺失 | Web S6-celebration 图 | 未验证 |

表内 `implementation/` 路径均相对 `output/playwright/gemsteps-ios/`。

已执行的局部修正：从运行中 Web 的 CSS 自定义变量读取 Lab 色值，经浏览器 Canvas
转换 sRGB；发现首稿 blue-400、yellow-400、indigo-600 和 rose-500 有差异，已更新。
修正后语法／卡片 typecheck 重验；**尚未进行原生视觉复测**。主要色值见证据索引。
卡片与页头的 SwiftUI 插值、字体度量、默认字号长标题、阴影与粒子缓动仍需截图或录屏比较；
不能提前标记为“已接受差异”。原生安全区、键盘、emoji 栅格化与音频路由按第4节记录。

第 5 节问题已异步询问，尚未收到答复：最低 iOS 17 与保持 Web 排布作为当前构建草案，
不视为用户已确认实际设备兼容；额外“积分不足，无法撤销”提示尚未启用。当前保留卡片可点、
本地拒绝且无额外提示、余额和次数不变，符合 Web 最终结果；本地保存前拒绝避免负余额。

下一步：安装完整 Xcode 与 iOS runtime 后，按 README 运行 App / XCTest；先处理实际编译或
持久化失败，再按 S0–S6 在相同安全区内容尺寸完成 V1–V7 成对截图与 F1–F10 操作。
继续“记录差异 → 局部修复 → 受影响场景复测”；目前环境阻塞处没有宣称循环完成。

清理记录：本次独立 Playwright 会话已关闭，临时 Next 服务已停止，测试容器已停止并移除。

### 继续准备环境（用户解锁后）

2026-09-20 用户要求自主准备并验证环境。用户已解锁 Mac，已通过 App Store
点击官方 Xcode 的重新下载入口，下载进行中；未要求用户自行安装。
不再把“尚未安装”当作无法继续尝试的结论。补充了保存后交互拦截、动作前跨日校验、
日期回拨和约18,250条逐笔记录启动读取的 XCTest，等待原生环境执行。
既有未验证标记保留，直到有实际构建／测试／模拟器证据。


环境进展：Xcode 27 已由 App Store 安装完成（界面显示 Open）。首次启动显示
《Xcode and Apple SDKs Agreement》，已向用户请求在这一具体协议步骤确认；尚未点击 Agree。
原生构建／运行仍未验证。新增3项后 XCTest 共10个测试方法，等待执行。
源码检查另修正了 iOS 不支持的 `onExitCommand`，改为关闭按钮的
`keyboardShortcut(.cancelAction)`；Escape 实际表现待 iOS 验证。

用户已明确同意 Xcode 许可协议，已点击 Agree。随后 Xcode 许可窗口按钮禁用，
等待系统授权；电脑操作工具拒绝访问受保护的 SecurityAgent。已请用户仅在系统窗口
完成密码／Touch ID 授权，不收集密码。尚未开始原生测试，不将点击 Agree 视作组件配置完成。


系统授权由用户完成，必需组件安装成功；`xcodebuild -version` 为 Xcode 27.0 / 27A266a。
已完成 iOS Simulator 通用目标 `build` 与 `build-for-testing`，均成功；唯一构建提示是
未链接 AppIntents 而跳过元数据提取，不影响本应用。构建日志在 `output/playwright/gemsteps-ios/native/`。
iOS 27.0 Simulator (24A434) arm64 runtime 下载中，测试尚未运行。
Xcode 首次设置的额外外部代理访问选择 Never；本任务用命令行和现有电脑操作工具。


第一轮10项原生 XCTest 暴露两个问题：只读保存失败后 rollback 的上下文仍查询到
未保存条目；主线程音频启动阻塞庆祝时限。已局部修复：失败后重建同一容器的唯一写入
ModelContext，保留显式 rollback；音频移到独立 actor，遮罩用动作时确定的
ContinuousClock 截止时间。原架构、数据库模型与产品范围不变，没有新增依赖。
复测10/10通过（`ios/NativeTestsRetry.xcresult`，文本日志 `native/tests-passed.log`）。
18,250条记录的真实 SQLite 打开与分批汇总首轮0.156秒；此数只代表本机模拟器测量。
损坏文件测试触发的 CoreData 错误及只读写入错误是预期故障注入，不是忽略的测试失败。


### 模拟器实际对照（继续进行）

iPhone 13 / iOS 27，业务时间固定 D=2026-09-20T19:00:00Z。
实际屏幕390×844，安全区内容390×763；Web viewport设为390×763。
原图1170×2532，比较图仅裁去顶部47pt／底部34pt安全区并等比缩至1x。
Web仍保留锁按钮，iOS按已批准范围删除锁入口；没有为比较修改Web布局。

已逐次点击确认：S1洗脸2／刷牙1，总分5；S2兑换棒棒糖到0、兑换次数1、再次禁用；
零余额撤销刷牙无变化；退款到5，撤销洗脸到4。未完成任务撤销禁用；跨标签保持撤销模式。
临时空值、0、1.5、101拒绝；余额4时减5拒绝；减2、加10到12，卡片次数保持1／1。
杀进程重开保留12与次数，回到正常任务页。固定次日重开，12余额保留、当天0、次数0；
次日兑换后7／−5。对应Web使用隔离 acceptance 测试库重放，S1–S5截图保存于native目录。
这不代替前台计时跨午夜与后台恢复检查。

视觉循环：首轮发现任务分数胶囊偏矮、标签字号偏大，已调整36pt胶囊和18pt标签，
复拍S0首张卡片y=209与Web一致。S5成对图又发现系统plain按钮额外淡化禁用内容，
改用仅返回label的卡片ButtonStyle，保留Web显式0.55透明度；正在构建复测。
尚未验收平板／窄屏／横屏／大字号、庆祝定时帧、真机听音。


继续完成：iPad 834×1194与Web安全区内容834×1142对照，四列位置、行距匹配；
iPhone SE 375×667任务单列匹配（比较内容375×646，顶部20pt安全区及底部1pt裁去，未拉伸）。
iPad辅助功能中号字体实测，长标题换行、卡片增高可见。
默认禁用修复后又发现透明度逐层合成问题，已在显式0.55之前加入compositingGroup；
最新构建通过，但奖励页最终复拍尚未完成，不能把前一次截图算最终验证。

为了验证实际前台10秒检查，增加仅Debug的 `-gemsteps-date-advances` 启动参数，
从指定业务日期按真实经过时间前进，Release无此入口。截图实际验证午夜前后
9月20日／+12 → 9月21日／−5，总余额均7，正确恢复已存在的次日记录。
日期回拨也实际截图确认：总余额7、旧日+12及洗脸／刷牙1／1，未重复奖励。

当前外部阻塞：Mac再次自动锁屏，CUA两次明确返回locked；已异步请用户解锁，
不尝试绕过登录。等待期间已完成上述命令行截图、尺寸核对和文档整理。
剩余全部任务滚动、平板奖励／弹层／横屏、最新禁用修复复拍、后台恢复、
弹层背景／Escape、部分快捷与100边界UI、离线系统操作、0.7秒动画帧和真机听音未验证。
完整证据和逐项限制：[原生验收索引](../output/playwright/gemsteps-ios/native/README.md)。
本轮未提交、推送、部署、发布，未改Web产品或Android代码。


### 本轮最终执行状态（以此处与native索引为准）

用户再次解锁后继续完成32任务滚动、9奖励、平板横屏七列、窄屏奖励双列、
平板居中弹层、全部快捷值、1／100边界、背景取消、实际后台跨午夜及三类庆祝。
主业务序列S0–S5和独立0→100→101→96动画序列均对照隔离Web，未改Web产品代码。
最后再次锁屏，尚未完成项如实保留，不因代码／构建／测试通过而整体通过。

| 验收步骤 | 当前结论 |
|---|---|
| 1 工程和固定界面 | 已实现；三模拟器启动、V1–V4／V7主要静态对照通过 |
| 2 本地业务闭环 | 已实现；F2–F8主要业务／故障／跨日通过；F1真机飞行模式仍未验证 |
| 3 视觉与反馈 | 已完成多轮局部修复并复拍，V1–V7有证据；F9真机听音与部分系统适配未验证 |
| 4 有限验收与交付 | 交付可运行工程与证据；**整体验收未完成**，没有把剩余限制标为通过 |

最新原生测试10/10（NativeTestsCurrent.xcresult、native/tests-current-passed.log）。
之后仅修改卡片层级／灰色与弹层行高／阴影；最终build通过，改动界面复测通过。
新修复：卡片透明度分组，奖励图片覆盖角标，弹层最小行高／白底合成，均有修复后截图。
三类动画从录像提取成功后约700ms画面；测得任务遮罩2.265秒、奖励2.205秒，
含帧采样误差，与2.2秒目标相符。未把录像当音效听感证明。

剩余可见差异：系统字体数像素度量、emoji栅格化、渐变插值、阴影及粒子轨迹；
原生安全区与键盘差异；Web压缩图比原始PNG模糊。所有差异及必验未验证项均列于
[最终证据索引](../output/playwright/gemsteps-ios/native/README.md)。

运行：Xcode打开ios/GemSteps.xcodeproj，scheme GemSteps，选专用模拟器后⌘R；⌘U测试。
完整命令见ios/README.md。依旧main工作，无提交、推送、部署或发布。

最终清理：专用Playwright会话关闭、localhost开发服务停止、一次性acceptance数据库容器停止并移除。模拟器和Xcode保留以便运行；没有后台任务或生产连接。

用户后续明确表示不必继续比较特别细的地方。本轮停止字体数像素、阴影与粒子轨迹的细节微调；保留已完成的功能与可用性验证，未验证项仍如实列出，不将此指示视为它们已通过。

### 提交授权

用户确认当前效果不错，并明确授权将本轮全部改动提交。将原生工程、资源、测试、执行记录及对照证据一并提交到main；仍不推送、部署或发布，剩余验收限制保持原记录。


## 9. 原生风格改造（2026-09-20）

用户已明确授权执行；主 checkout main，开始时无工作区改动。本轮未提交、推送或发布。

已完成实现：
- PointsView 使用系统 NavigationStack / TabView，手机固定两列、iPad 按窗口宽度增加列数。
  卡片为 LazyVGrid 中的原生 bordered Button；两类目录共享排布。删除 CardLayout。
- 删除 Web 色值、渐变数组、按 ID 长度取色、Arial、手绘金币、图片 ID 尺寸分支、
  自定义空 ButtonStyle、640 断点、强制浅色。使用系统颜色、字体和按钮状态。
- AdjustmentView 改为 sheet / Form / Picker / TextField / toolbar；保留数值与余额校验，
  保存失败保留表单。快捷分值在大字号下可纵向排列。
- CelebrationView 改为系统 fullScreenCover，使用不透明系统背景、大图／emoji、
  大号分数及 SwiftUI spring；Reduce Motion 使用淡入。无自定义粒子或轨迹计时。
- 保留庆祝期间的业务重入保护与全屏遮挡；声音与约 2 秒展示从全屏出现开始，
  临时加减表单 onDismiss 后才呈现。奖励也使用自己的图片。撤销维持静默。
- 删除手工 displayedPoints 递进任务；直接绑定真实余额并使用 numericText。
- 删除 ascending.wav / descending.wav，替换为 Kenney CC0 confirmation_002 / 004
  的 PCM WAV 转码，不合成新旋律；保留 AVAudioPlayer 原生播放，许可与来源随资源保存。
- 数据模型、目录、余额与次数规则、时区、跨日及保存失败回滚未更改。

验证：
- Xcode 27 / iOS 27 构建成功，12 项 XCTest 全部通过（原有 10 项＋2 项新回归）。
- 回归包含：庆祝完成前拒绝重复动作、等待表单关闭不消耗庆祝时间、奖励图片、
  过期呈现 ID 不关闭新庆祝、两份新音效可解码且旧音效已不在应用 bundle。
- iPhone 13 与 SE 首屏确认两列，iPad 11 竖屏四列、横屏六列；原数据库升级后正常读取。
- iPhone 深色及最大辅助字号检查完成，卡片保留两列并增高，分值／次数改为纵向排列。
- 实际点击通过：任务加分（7→8）、临时 +10（8→18）、101 非法输入留在表单并提示、
  改回合法值提交、奖励兑换（19→9）、撤销兑换（9→19，不庆祝）；不足余额奖励禁用。
- 实屏发现 sheet 与 fullScreenCover 共用呈现容器时，临时调整庆祝可能被表单关闭吞掉。
  已用独立稳定呈现容器修复，随后复测普通提交、快捷分值、键盘打开／收起后提交，
  均看到全屏星星与分数并自动恢复。无固定延迟重试或新依赖。
- 生成的验收图片与修复后录屏保存在 `ios/DerivedData/NativeStyleReview/`（已忽略，不入 Git）。
- iPad 横屏系统 sheet 居中呈现，临时减分 96→95 后全屏覆盖与恢复通过。
- 修复后再次运行全部 12 项 XCTest，零失败；`git diff --check` 通过。
- 验收不再对照 Web 截图；旧证据仅留作历史记录。

仍待验证：真机听感、静音／蓝牙路由、触觉、VoiceOver、系统减弱动态效果实机表现、
iPad 窄窗口及 iOS 17 实际运行。本轮开始时锁屏限制已解除，已完成上述模拟器点击验收；
不将模拟器、构建或单元测试当作真机／最低系统版本项目通过。


### 头部排版修正（2026-09-20）

用户明确要求原生组件保留原来的紧凑横向组织。本轮只调整 PointsView 头部：
删除主页面大号导航标题及其工具栏留白；余额在左，日期和今日净值在右。
手机按钮另排一行，iPad 宽屏信息与按钮同排；使用 ViewThatFits 按内容是否放得下
选择排布，大字号或长数值才允许信息换行，没有恢复 Web 固定断点或颜色补偿。
方块、庆祝、弹窗及业务规则未修改，保留前轮未提交改动。

验证：Xcode 27 模拟器构建通过；已检查 iPhone 13、SE 和 iPad 横屏截图，
默认字号下手机信息横排、按钮第二行，iPad 单行。图片见
`ios/DerivedData/NativeStyleReview/iphone-header.png`、`iphone-se-header.png`、`ipad-header.png`。
此次为布局修正，未新增或重复执行业务单元测试；Mac 再次锁屏，未重复点击验收。
开始时磁盘满，清理的仅为本任务可重新生成的编译中间文件和临时录屏，未删数据或源码。


### 页面结构对照修正（2026-09-20）

用户指出 TabView 把入口移至手机底部／iPad 顶部，要求实际与 Web 比较结构后再报告完成。
本节取代前述 TabView 决策。保留所有此前未提交的原生改造。

实现：删除 TabView，单个 ScrollView 按以下顺序呈现：头部信息与操作 → 错误／撤销提示
（有则显示）→ 两个等宽的任务／奖励按钮 → 当前目录的 LazyVGrid。两个按钮分别使用
系统 borderedProminent / bordered 样式表示选中与未选中，并保留 VoiceOver selected trait。
日期／星期／当日净值恢复为同一横排；积分为 SF Symbol 星标＋数字；默认字号下次数回到
卡片右上角。辅助字号时次数允许移到卡片内容区，以避免遮挡。没有恢复 Web CSS 模拟。

对照依据：本轮实际读取 `web/src/app/PointsPage.tsx`，确认 Web 工作区没有修改；查看
既有 Web 验收图 `native/web-S1-390x763.png`、`native/web-ipad-S0-834x1142.png`、
`native/web-ipad-landscape-1194x782.png`（均位于 `output/playwright/gemsteps-ios/`）。
新截图为 `ios/DerivedData/NativeStyleReview/*-structure.png`。

| 结构项 | Web | iOS 当前任务页实屏 | 结论 |
| --- | --- | --- | --- |
| 第一行 | 左积分；右日期、星期、当日净值 | iPhone 13／SE 与 iPad 横屏均相同组织 | 一致 |
| 操作入口 | 手机第二行；平板与信息同排 | 同样排列撤销／临时加减 | 一致，锁定按此前授权不移植 |
| 任务／奖励 | 头部下方两个等宽按钮 | 同位置同顺序；无系统顶部或底部标签栏 | 一致 |
| 内容区 | 切换按钮下面显示当前任务或奖励网格 | 同位置显示当前目录网格 | 任务页已实屏核对，奖励切换点击待验收 |
| 卡片信息 | 中央图片、名称、分值；右上角次数 | 默认字号下同样排列 | 一致 |

这里比较的是页面层级、控件位置与信息组织，不宣称像素／列数完全相同。
保留用户已明确要求的手机两列与平板自适应列数，因此不恢复 Web 在 375 宽时任务一列、
奖励不同列宽或横屏七列挤压的规则。系统颜色、字体和按钮形状也保持原生。

构建及 12 项 XCTest 通过（零失败）；已实际查看 iPhone 13、SE、iPad 横屏新截图并与上述 Web 图比较。
本轮 Mac 仍锁屏，已向用户请求解锁；奖励页实际切换、iPad 竖屏截图与交互验收尚未完成，
不将静态截图／源码对照表述为这些交互已通过。


### 原生配色预览（2026-09-20）

用户要求保留原生组件，用接近 Web 的多彩外观制作预览，无需精确还原色值。
沿用上一节的页面结构：紫／靛蓝原生 LinearGradient 页头、白色头部文字、黄色 SF Symbol
积分图标；任务／奖励用靛紫 tint 的系统选中按钮。卡片使用八色循环的 borderedProminent
系统按钮，白色文字与半透明分值底色；有次数时右上角用绿色突出，零次用深色半透明底。
颜色独立于 Web CSS 和项目 ID；没有恢复特殊网格、字体或素材尺寸分支。
页面使用系统背景叠加轻微紫色渐变，自动适配深浅色。庆祝、声音和业务未改。

本轮为配色预览，构建和 diff 检查通过；完整交互验收按用户要求留待稍后。
已实际查看 iPhone 13 浅色／深色与 iPad 竖屏浅色截图：手机两列、iPad 四列，
头部与任务／奖励入口顺序保持不变。截图保存在忽略的 DerivedData/NativeStyleReview 目录。
